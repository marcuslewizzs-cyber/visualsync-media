import { QuoteWizard } from "@/components/quote-wizard"

export const metadata = {
    title: "Create Order | Quote Wizard",
    description: "Create a new production order with our Quote Wizard",
}

export default function CreateOrderPage() {
    return <QuoteWizard />
}
